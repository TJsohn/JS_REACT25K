/* document.querySelectorAll('input[type="checkbox"]').forEach((checkbox) => {
  checkbox.addEventListener("change", totalPriceDisplay);
  if (this.checked) {
      console.log(`${this.value} is checked`);
    } else {
      console.log(`${this.value} is unchecked`);
    
});
console.log(totalPriceDisplay); */
/*
const inputType = document.getElementById("type");
const displayPrice = document.getElementById(`${totalPrice} €`);

const chosenPancake = document.getElementById("type").value;

document.getElementById("type").addEventListener("change", chosenPancake);

document.querySelectorAll('input[type="checkbox"]').forEach((checkbox) => {
  checkbox.addEventListener("change", function () {
    if (this.checked) {
      console.log(`${this.value} is checked`);
    } else {
      console.log(`${this.value} is unchecked`);
    }
  });
});*/

/* const updateDisplayPrice = () => {
  displayPrice.innerText = inputType.value;
};

inputType.addEventListener("change", updateDisplayPrice); */
